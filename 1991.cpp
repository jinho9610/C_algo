#include <iostream>
#include <functional>
#include <algorithm>
#include <string>
#include <queue>
#include <vector>

using namespace std;

class Node
{
public:
    int data;
    Node *left;
    Node *right;
};

typedef long long ll;

int n;

int main()
{
    cin >> n;
    char p, l, r;
    for (int i = 0; i < n; i++)
    {
        cin >> p >> l >> r;
        Node parent = Node()
    }

    return 0;
}